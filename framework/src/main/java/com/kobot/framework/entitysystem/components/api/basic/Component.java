package com.kobot.framework.entitysystem.components.api.basic;

/**
 * By default many instances of given Component Class can be added to same Entity.
 * By default these instances cannot be shared among Entities.
 * eg. Weapon
 * Entity can have multiple weapons but each entity must has its own instances of weapons.
 */
public interface Component {
}
